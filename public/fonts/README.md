Place the self-hosted Pretendard variable font at:

- `public/fonts/PretendardVariable.woff2`

The global CSS already declares `@font-face` and falls back to the centralized Korean/system font stack when this file is not present.
