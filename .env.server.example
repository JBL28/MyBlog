# Copy this file to .env.server on the server and edit the values.
# App and db containers communicate through the Docker Compose network.
# DB_HOST must match POSTGRES_CONTAINER_NAME when using the container-name:port style.

COMPOSE_PROJECT_NAME=myblog
MYBLOG_IMAGE=myblog:latest
APP_PORT=3000

POSTGRES_VERSION=16-alpine
POSTGRES_CONTAINER_NAME=myblog-db
POSTGRES_DB=myblog
POSTGRES_USER=myblog
POSTGRES_PASSWORD=replace-with-a-strong-password

DB_HOST=myblog-db
DB_PORT=5432
